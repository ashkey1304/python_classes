# Write a Python program to count the number of characters (character frequency) in a string.
import counter
input_string = input("Enter the string:")
char_count = counter(input_string)
print("the number of character in a string.")